% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ebrel-package.R
\name{create_ebrel_class_object_R}
\alias{create_ebrel_class_object_R}
\title{Create an ebrel data object}
\usage{
create_ebrel_class_object_R(
  E,
  C,
  SD,
  D,
  SxH,
  O,
  dim_x,
  dim_y,
  n_h,
  n_s,
  sentinel = 1e+10,
  sigma
)
}
\arguments{
\item{E}{Numeric vector of length \code{n_cells * n_h}; encodes current habitat states.}

\item{C}{Numeric vector of length \code{n_cells * n_h}; encodes per-cell, per-habitat costs.}

\item{SD}{Numeric vector of length \code{n_cells * n_s}; encodes current species/feature distribution.}

\item{D}{Numeric vector of length \code{n_s}; species/feature dispersal potential (e.g., max distance).}

\item{SxH}{Numeric vector of length \code{n_s * n_h}; species–habitat association strengths.}

\item{O}{Numeric vector of length \code{n_s}; species/feature targets (e.g., proportional increase in occupancy).}

\item{dim_x}{Integer; number of grid cells along the x/longitude/easting dimension.}

\item{dim_y}{Integer; number of grid cells along the y/latitude/northing dimension.}

\item{n_h}{Integer; number of habitats considered in spatial prioritisation.}

\item{n_s}{Integer; number of species/features considered in spatial prioritisation.}

\item{sentinel}{Numeric; sentinel value for missing/unavailable cells.}
}
\value{
An list with the data needed to run ebrel.
}
\description{
Initialises and returns an \code{ebrel} object used by \code{run_ebrel_R()}.
}
\details{
Let \code{n_cells <- dim_x * dim_y}. Vectors are flattened arrays with the
indicated lengths:
\itemize{
\item \code{E}: cell × habitat (length \code{n_cells * n_h}); stacked by habitat within cell,
i.e., index \code{cell + n_cells * (h - 1)}.
\item \code{C}: cell × habitat costs (length \code{n_cells * n_h}); same layout as \code{E}.
\item \code{SD}: cell × species baseline distribution (length \code{n_cells * n_s}).
\item \code{SxH}: species × habitat association scores (length \code{n_s * n_h});
column-major flattening of an \code{n_s × n_h} matrix, i.e., index
\code{sp + n_s * (h - 1)}.
}
Cells with no data/unavailable should be marked with \code{sentinel}.
}
\examples{
\dontrun{
n_x <- 100; n_y <- 80
obj <- create_ebrel_class_object_R(E, C, SD, D, SxH, O, n_x, n_y, 5, 12)
}
}
\seealso{
\code{\link[=run_ebrel_R]{run_ebrel_R()}}
}
